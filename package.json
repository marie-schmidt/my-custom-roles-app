{
  "name": "roles-function",
  "version": "1.0.0",
  "scripts": {
    "start": "swa start frontend --api-location api",
    "playwright_test": "playwright test",
    "install": "npm install",
    "build": "npm run build"
  },
  "devDependencies": {
    "@playwright/test": "^1.22.2"
  },
  "dependencies": {
    "build": "^0.1.4"
  }
}
